{
  "name": "hugo-blox-blog-starter",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "description": "Blog starter template for Hugo Blox with Tailwind CSS v4",
  "scripts": {
    "dev": "bash -c 'if [ -f .env.local ]; then set -a; source .env.local; set +a; fi; hugo server --disableFastRender'",
    "build": "hugo --minify"
  },
  "pnpm": {
    "overrides": {
      "picomatch": "2.3.2"
    },
    "allowedBuildScripts": [
      "@parcel/watcher"
    ],
    "onlyBuiltDependencies": [
      "@parcel/watcher"
    ]
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "@tailwindcss/typography": "^0.5.19",
    "preact": "^10.27.3",
    "tailwindcss": "^4.1.17"
  }
}
